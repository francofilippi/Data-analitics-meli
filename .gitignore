# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Datos generados (se regeneran con data/generar_datos.py)
data/*.csv
data/*.parquet

# Respaldo durable del histórico (SÍ se versiona: es el backup del YoY).
# Las reglas de arriba solo aplican a hijos directos de data/, no a subcarpetas,
# pero lo explicitamos por las dudas.
!data/historico/
!data/historico/**

# Streamlit
.streamlit/secrets.toml

# Entorno
.env
secrets.toml
